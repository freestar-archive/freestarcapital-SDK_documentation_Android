// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        google()
        jcenter()
        
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.0.1'
        
        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        maven { url 'https://jitpack.io' }
        maven { url "https://adcolony.bintray.com/AdColony" }
        maven { url "https://dl.bintray.com/vdopiacorp/fsdk" }
        maven { url "https://s3.amazonaws.com/moat-sdk-builds" }
        maven { url 'https://tapjoy.bintray.com/maven' }

        maven {
            url  "https://timehop.bintray.com/nimbus-android"
            credentials {
                username "freestar-stocktwits@timehop"
                password "82d36f46b76d8f98f10536ec41463157fd724574"
            }
            metadataSources {
                mavenPom()
                artifact()
                ignoreGradleMetadataRedirection()
            }
        }
        maven {
            url  "https://timehop.bintray.com/nimbus-openrtb"
            credentials {
                username "freestar-stocktwits@timehop"
                password "82d36f46b76d8f98f10536ec41463157fd724574"
            }

            metadataSources {
                mavenPom()
                artifact()
                ignoreGradleMetadataRedirection()
            }
        }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
